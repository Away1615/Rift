# 如何制作连贯而流畅的运动系统

1. 在角色开始移动时，添加起步动画过渡，根据当前角色的速度方向和旋转方向选择对应方向的起步动画

2. 停步动画，在 move 和 idle 状态下添加停步状态作为过渡（使用 distance match来匹配动画和停步距离。）

3. 折返动画（pivot 状态 和 Sprint 状态进行双向连接），当角色的加速度与速度方向相反时，进入 pivot 状态，我们可用加速度与速度的点乘积的值来判断二者的方向是否反向。鼠标的旋转同样可以实时改变加速的方向。如果我们希望玩家移动鼠标达到的效果是“转弯”而非“折返”，我们需要在旋转镜头时，平滑加速向变化率。使在该情况下加速度和又的夹角不会突为反向。平滑的核心逻辑是记录下当前帧角色控制的旋转并用插值的方式让加速度的朝向按照一定角速度逼近控制器的旋转方向。这样处理会增加角色的转弯半径，但也增加了在转弯过程中混合“转弯倾斜”的过度空间。我们可以利用加速度制器旋转时的角度插值制作倾斜奔跑的混合空间。这样可以使角色在运动转弯时，身体向弯道内侧倾斜并保证运动的连贯性。
4. 跳跃衔接：使用 叠加 additive动画来实现落地衔接过程。（可以使用第三人称自带的落地动画）

5. 闪避动画，提取闪避动画的位置曲线，根据曲线值计算闪避过程中的瞬时加速度并叠加给当前角色，避免跑步和闪避动画衔接处的停顿感

# 如何制作连招系统

1. 拆解一个攻击动作
分为三部分。攻击前摇，攻击区间，攻击后摇。在攻击后摇的时间区间内判断是否有输入，则触发下一段攻击。（参考 combo graph 插件实现，窗口区间内检测是否有连招图内配置的输入相应）,一旦接收到指定输入，则进入一段连招。轻连击，重连击，以及轻转重

2. 重置区间

在攻击后摇区间插入一个重置节点，该节点可以将攻击后摇分割为连招允许区域以及重置区域。当动画进入重置区域时，任意输入都可以中断当前动画播放，并执行新的输入响应，但连招会重置。动画攻击区间结束后，会在最后挥舞的pose处停留大概 10帧，在 pose帧内衔接连招动画，可以使连招过度更为自然。

3. 输入缓存

当动画执行到该区时。玩家的操作会被记录下来，但不会响应。直到动画进入连招允许区间时，自动执行所记录的输入。 一般通过动画通知状态开始时重置记录的缓存，Tick 过程中更新记录所需要的输入，结束时执行所记录的输入，可以通过增强输入系统的 inject Vector for action 实现模拟输入

4. 闪避取消

停止当前正在播放的蒙太奇。可以在使用翻滚后的短时间内保存连招记录，保证玩家连招体验。

# 如何增强战斗打击感

1. 添加拖尾特效
2. 添加攻击音效
3. 添加被打角色的受击动画（点乘和叉乘方法计算受击方向，并播放相应方向受击动画）
4. 当受击的 pose 帧与 idle 帧差异较大时，可以降低受击动画的混入时间
5. 添加受击特效
6. 添加受击音效
7. 添加攻击震屏
8. 添加攻击顿帧
- 攻击命中时，降低角色当前的蒙太奇模仿速率，固定延时后回复正常速率：播放速率吧，延迟时间；播放速率越小，停顿感越明显，延迟时间越久，粘滞感会更强。
- 抽帧实现，将蒙太奇暂停。延迟一小段时间后，跳过延迟帧继续播放动画（强化攻击的卡肉）
- 大型怪物不会有明显的受击动画，我们可以通过局部抖动其骨骼强化民众反馈
- Sequence 运镜

# 资源条设计

1. 通过连续进攻积攒战斗资源，消耗资源获得一定的战斗优势奖励（参考怪物猎人 太刀气刃槽资源循环）

2. 限制型资源条，玩家可连续执行的行为次数收到资源条上限的控制

使用 GAS统一管理玩家属性，可以使用 GSCAttributeSet （已经预设了血量蓝量耐力等，通过 DataTable 配置数据），当玩家出招命中时，结算一个能够恢复资源值的 GameplayEffect。通过 setByCaller 的方法 单独配置每个招式的回复值

浪人崛起闪刃，怪物猎人太刀气刃

# 怪物 AI 战斗设计
 
1. 通过 controller 和 行为树对怪物进行配置

- 通过 AI 感知组件进行监听，发现玩家进入战斗状态并尝试发起攻击

2. 怪物集群

- 当 1个怪物和玩家对峙时，其余怪物在旁边周旋

- 玩家与怪物 最小距离：怪物后退，最大距离，怪物靠近，适当距离，怪物周旋。

3. 怪物连招

- 在怪物攻击动画中，定义了动画通知窗口，在窗口生效期间，根据战场信息和招式属性决定怪物是否有下一段连招，连招是什么，然后传递给行为树并执行

- 比如设计怪物仅在攻击后摇时被击打才会硬直，通过动画通知检测是否被击，行为树内，将播放受击动画的自定义事件绑定到收到任意伤害时，然后播放受击动画并暂停行为树

- 怪物需要对玩家的特定行为进行监听并打断。启动行为树的监听行为，监听玩家的攻击指令和回复指令。在周旋状态下也可以监听玩家的攻击指令，如果玩家发动攻击，怪物提前结束周旋并发起新一轮进攻。

# 韧性系统

- 玩家和怪物收到彼此攻击时，韧性值会被削减

- 怪物韧性被清空时，会进入硬直受击状态

- 一段时间未受到伤害，怪物韧性会开启自动回复

- 进入受击状态，怪物会立刻恢复满韧性

可以参考黑暗之魂1 中 削韧公式

极限闪避可以增强削韧


# AAA 游戏镜头

相机管理系统，根据不同的环境和玩法，相机会切换到不同的机位展示重点

- 常规模式：弹簧臂3米，相机轴心点在角色头部

- 进入狭小环境：缩短弹簧臂长度，将角色置于镜头左侧，一般还要限制玩家的控制器旋转角度

- 通过峭壁时：相机聚焦于峭壁底部，凸显位置高度。可以设置石块等，通过 timeline 将其进行俯仰偏转来观察石块掉落轨迹

- 角色转弯或需要镜头稍微延迟：开启镜头的跟随延迟实现

- Boss 战：从当前位置 sequence 推进，给 boss 特写镜头